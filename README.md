# PDF 합치기 + 용량 줄이기

PDF 파일 여러 개를 올린 순서대로 합치고, 글자 품질을 최대한 유지하면서 용량을 줄이는 웹앱입니다.

## 기능

- PDF 여러 개 업로드
- 업로드된 순서 그대로 병합
- PDF 최적화로 용량 줄이기
- 결과 PDF 다운로드

## 실행 방법

```bash
pip install -r requirements.txt
streamlit run app.py
```
